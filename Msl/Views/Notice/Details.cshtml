@model Msl.Models.Student

@{
    ViewData["Title"] = "Notice Details";
}

<div>
    <h4>Notice</h4>
    <hr />
    <div>
        <div class="h4">
            @Html.DisplayFor(model => model.Sabject)
        </div>
        <div class="">
            <p>
                @Html.DisplayFor(model => model.Body)
            </p>
        </div>
        
    </div>
    
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
