@model IEnumerable<Msl.Models.Employment>

@{
    ViewData["Title"] = "Employment";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="nav-link d-inline"> <i class="fa-solid fa-plus"></i> Employment</a>
    <a class="nav-link d-inline" asp-area="" asp-controller="Employee_Details" asp-action="Index">Back to List</a>
</p>
<table class="table" id="myTableEmployment">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ApplicationUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Designation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.From)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.To)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
             <td>
                  @Html.DisplayFor(modelItem => item.ApplicationUser.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Designation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.From)
            </td>
           @if(item.To=="true"){
                    <td>
                        Continue
                    </td>
                }
                else
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.To)
                    </td>
                }
                
           
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
@section Scripts{
        <script>
            $(document).ready(function () {
                var today = new Date().toISOString().split('T')[0];
            $('#myTableEmployment').DataTable({

                }
                );
            });

        </script>
}